rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /reports/{reportId} {
      // Zugriff für Praktikumsberichte (Schülerkürzel & Lehrer-Masterpasswort)
      allow read, write: if true;
    }
  }
}
